apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  labels:
    app: test  # Coincide con la etiqueta en el Deployment de Nginx para coherencia
spec:
  containers:
  - name: test-container
    image: busybox
    command: ["sleep", "3600"]  # Mantener el pod activo durante una hora para pruebas

