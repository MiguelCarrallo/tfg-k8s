grafana:
  service:
    type: ClusterIP
  adminPassword: admin
  sidecar:
    dashboards:
      enabled: true

prometheus:
  prometheusSpec:
    serviceMonitorSelectorNilUsesHelmValues: false
    storageSpec: null
