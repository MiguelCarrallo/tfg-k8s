apiVersion: v1
kind: Pod
metadata:
  name: cliente-a
  namespace: dev
spec:
  containers:
  - name: busybox
    image: busybox:stable
    command: ["sleep", "3600"]
    resources:
      requests:
        cpu: "50m"
        memory: "32Mi"
      limits:
        cpu: "100m"
        memory: "64Mi"
